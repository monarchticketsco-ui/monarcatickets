// Panel de organizador: eventos propios, ventas, saldo en payout_ledger.
// Protegido por RLS (organizers.owner_user_id = auth.uid()).
// TODO (Fase 4).
export default function PanelOrganizadorPage() {
  return <main><h1>Panel de organizador</h1></main>;
}
