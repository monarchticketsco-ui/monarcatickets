// Perfil de cliente: historial de compras, descarga de boletos y facturas,
// transferencia de boleto. Ver blueprint seccion 04 (modulos) — requiere
// sesion (Supabase Auth) con role = 'comprador'.
// TODO (Fase 4).
export default function MiCuentaPage() {
  return <main><h1>Mi cuenta</h1></main>;
}
