// Catalogo publico de eventos.
// TODO (Fase 1, blueprint seccion 02/06): reemplazar por consulta real a
// Supabase de `events` donde status in ('publicado','en_venta'), con
// disponibilidad en vivo via Realtime (Broadcast) sobre ticket_types.
export default function HomePage() {
  return (
    <main>
      <h1>Monarca Tickets</h1>
      <p>Catalogo de eventos — por construir en la Fase 1.</p>
    </main>
  );
}
